node_modules
dist
*.log
.DS_Store
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.wa-auth/
.proof-private/
docs/.scratch/
.artifacts/
apps/docs/.next/
apps/docs/.source/
apps/docs/out/
apps/docs/public/r/
apps/docs/tsconfig.tsbuildinfo
.pnpm-store/
.fallow/
.reviews/
examples/qr-cli/.data/

# Local environment. `.env.example` is the tracked template and is re-included
# below, so adding a real value to it is a visible mistake rather than a silent
# one.
.env
.env.*
!.env.example

# The Convex CLI writes generated bindings beside the functions it pushes. The
# conformance fixture never imports them -- it calls its deployment by function
# name -- so they are build output of running the suite, not source.
packages/whatsappd/tests/convex-app/convex/_generated/

# The pair-page example writes a linked credential here; never commit one.
examples/pair-page/.account/
examples/pair-page/qr.txt
