{
  "$schema": "https://raw.githubusercontent.com/kucherenko/jscpd/master/packages/jscpd/schema.json",
  "path": ["src"],
  "format": ["typescript"],
  "reporters": ["consoleFull"],
  "absolute": true,
  "gitignore": true,
  "minLines": 8,
  "minTokens": 60,
  "threshold": 0.3,
  "ignore": ["**/*.d.ts"],
  "//": "Duplication measured at 0.15% (one clone, 14 lines), so the threshold is 0.3% -- twice the floor, and low enough to bind. Set at 1% first, which let a deliberately pasted 60-line block through at 0.75%: a gate that cannot fail is not a gate.",
  "//interfaces": "`interface` and `type` bodies are excluded. The one clone jscpd found was WhatsAppSession's command surface against the internal socket port it delegates to -- two declarations that mirror each other on purpose, because the public surface restating the internal one is the boundary, not a shortcut someone took. Deduplicating those types would couple the published API to an internal port.",
  "ignoreCase": false,
  "blame": false,
  "silent": false
}
